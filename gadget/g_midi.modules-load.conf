# Install to: /etc/modules-load.d/g_midi.conf
# Loads the class-compliant USB-MIDI gadget early (systemd-modules-load.service, before
# basic.target) so the host enumerates the MIDI device within a few seconds -- independent
# of the sensor script. This is the whole "host sees the device fast" win.
g_midi
