# Install to: /etc/modules-load.d/g_midi.conf
# Loads the class-compliant USB-MIDI gadget early (systemd-modules-load.service,
# before basic.target) so the host enumerates the MIDI device within seconds --
# independent of the sensor script.
g_midi
